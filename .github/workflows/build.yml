name: Build
on: [workflow_dispatch]

jobs:
  build:
    name: kobo-tieng-viet
    runs-on: ubuntu-latest
    container: ghcr.io/rinlevan/kobo-tieng-viet:latest
    steps:
    - name: Checkout
      uses: actions/checkout@v5
      with:
        submodules: true
    - name: Build
      run: make all
    - name: Upload
      uses: actions/upload-artifact@v4
      with:
        name: kobo-tieng-viet
        path: ./dist/KoboRoot.tgz